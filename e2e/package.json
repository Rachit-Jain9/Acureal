{
  "name": "acureal-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Acureal end-to-end smoke tests (Playwright). Logs in as an isolated test account and click-tests the flows that broke in July 2026 — empty dashboard/deals, NaN chips, blank pages, broken exports — against a live deployment (PR preview in CI, production locally).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
