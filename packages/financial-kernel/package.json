{
  "name": "@redip/financial-kernel",
  "version": "0.1.0",
  "description": "Acureal canonical financial kernel — pure deterministic underwriting primitives, KPIs, and asset-class models.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "jest --no-cache",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "redip",
    "underwriting",
    "real-estate",
    "india",
    "financial",
    "deterministic"
  ],
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  },
  "overridesRationale": {
    "brace-expansion": "Same pin as backend/package.json: <=5.0.7 carries GHSA-mh99-v99m-4gvg (DoS), reached here through the jest toolchain's minimatch. This package's CI audit gate runs WITHOUT --omit=dev, so devDependencies count. npm's only offered remedy was jest@25 — a five-major downgrade. 5.0.8 is the patched line with an unchanged single-function API. Remove once jest's dependency tree moves."
  },
  "overrides": {
    "brace-expansion": "^5.0.9"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
